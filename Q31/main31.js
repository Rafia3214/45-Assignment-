"use strict";
let userNames = ['admin', 'Arooba', 'mahira', 'Hassan', 'Rashid'];
if (userNames.length === 0) {
    console.log(`We need to find some users!`);
}
else {
    userNames = [];
    console.log(`All user Names have been removed.`);
}
